version: "3.9"

services:
  research-agent:
    build: .
    ports:
      - "8501:8501"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      TAVILY_API_KEY: ${TAVILY_API_KEY}
    env_file:
      - .env
    volumes:
      - .:/app
